package com.cityfix.presentation.theme

import androidx.compose.ui.graphics.Color

// --- Brand ---
val BrandPrimary = Color(0xFF1565C0)     // deep blue
val BrandSecondary = Color(0xFF42A5F5)   // light blue

// --- Status ---
val StatusNew = Color(0xFFE53935)        // red
val StatusInProgress = Color(0xFFFB8C00) // orange
val StatusResolved = Color(0xFF43A047)   // green

// --- Surfaces ---
val CardBackground = Color(0xFFF5F7FA)

// --- Theme palette (kept for the Material3 ColorScheme wiring) ---
val PrimaryBlue = BrandPrimary
val PrimaryBlueDark = Color(0xFF003C8F)
val SecondaryTeal = Color(0xFF00897B)
val TertiaryAmber = Color(0xFFF57C00)
